type Query {
  reviews(bookId: Int): [Review!]!
  topBooks(limit: Int): [Book]
}
type Mutation {
  addReview(memberId: Int!, bookId: Int!, rating: Int!, comment: String!): Review
}
type Review {
  id: ID!
  book_id: Int!
  member_id: Int!
  rating: Int!
  comment: String
}